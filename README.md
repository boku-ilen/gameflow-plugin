# gameflow-plugin
A minimalist game mode / game state addon for Godot.

The addon itself is in the `addons/gameflow` folder. The rest is an example project that shows how the GameMode and GameState nodes can be used.
